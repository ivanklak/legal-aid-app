import React from "react";
import section_styles from '../section/Section.module.css';

const Section = () => {

    return (
        <>
            <section className={`${section_styles.home_section}`}>

            </section>
        </>
    );
}

export default Section;