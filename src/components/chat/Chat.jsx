import React from "react";
import styles from '../../App.module.css';

const Chat = (props) => {
    return (
        <>
            <section className={`${styles.home_section}`}>
                <h1>Chat</h1>
            </section>
        </>
    );
}

export default Chat;